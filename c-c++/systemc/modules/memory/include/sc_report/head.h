// SystemC core
#include <systemc.h>        // SystemC main header

// Standard C++ utilities
#include <iostream>         // std::cout, std::cerr
#include <fstream>          // file input/output streams
#include <sstream>          // stringstream
#include <string>           // std::string
#include <vector>           // std::vector
#include <array>            // std::array if needed
#include <map>              // std::map if needed
#include <algorithm>        // std::sort, std::find
#include <cassert>          // assert()
#include <type_traits>      // enable_if, is_integral, etc.
#include <limits>           // numeric limits
#include <cstdint>          // fixed width integer types
#include <cstring>          // memcpy, memmove
#include <bitset>           // std::bitset for bit manipulation
#include <cmath>            // math functions
#include <set>            // std::set for unique collections
#include <unordered_map>   // std::unordered_map for fast lookups
#include <unordered_set>   // std::unordered_set for fast unique collections
#include <tuple>            // std::tuple for multiple return values
#include <functional>      // std::function for callbacks
#include <optional>        // std::optional for optional values
#include <variant>         // std::variant for type-safe unions
#include <memory>           // std::shared_ptr, std::unique_ptr
#include <stdexcept>       // std::runtime_error, std::logic_error
#include <random>         // Random number generation

// Optional for timing or concurrency (usually host-side)
#include <chrono>
#include <thread>

#include <regex>

